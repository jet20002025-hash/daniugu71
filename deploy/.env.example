# 复制为服务器上的 /var/www/stock-app/.env（权限 chmod 600）
# 生产务必修改 SECRET_KEY

SECRET_KEY=请改为随机长字符串
GPT_DATA_DIR=/data/gpt
STOCK_APP_DB=/data/users.db
ADMIN_USERNAMES=admin
# MAX_ACCOUNTS_PER_IP=1
# USE_SCAN_QUEUE=0
